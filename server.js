require('dotenv').config();const path=require('path');const express=require('express');const cors=require('cors');const cron=require('node-cron');const{initDb,db}=require('./db/database');const listingsRoutes=require('./routes/listings');const investorsRoutes=require('./routes/investors');const matchesRoutes=require('./routes/matches');const adminRoutes=require('./routes/admin');const outreachRoutes=require('./routes/outreach');const subscribeRoutes=require('./routes/subscribe');const{runMatchingForAll}=require('./matcher/engine');const app=express();const PORT=process.env.PORT||3001;initDb();app.use(cors());app.use(express.json({limit:'4mb'}));app.use(express.urlencoded({extended:true}));app.use(express.static(path.join(__dirname,'public')));app.use('/api/listings',listingsRoutes);app.use('/api/investors',investorsRoutes);app.use('/api/matches',matchesRoutes);app.use('/api/admin',adminRoutes);app.use('/api/outreach',outreachRoutes);app.use('/api/subscribe',subscribeRoutes);app.get('/api/health',(req,res)=>{res.json({ok:true,app:'DealMatcher',time:new Date().toISOString()})});const schedule=process.env.CRON_SCHEDULE||'0 7 * * *';cron.schedule(schedule,async()=>{console.log('[CRON] Running daily match');try{runMatchingForAll();console.log('[CRON] Done.')}catch(e){console.error('[CRON] Failed:',e.message)}});app.listen(PORT,()=>{console.log('\n🎯 DealMatcher running on http://localhost:'+PORT+'\n')});
